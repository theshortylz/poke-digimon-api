import { InputPort } from 'src/modules/common/ports/input-port';

// Pokémon-specific input port that extends base functionality
// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface PokemonInputPort extends InputPort {
  // Pokémon-specific input methods can be added here if needed
}
