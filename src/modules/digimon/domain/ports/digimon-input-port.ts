import { InputPort } from 'src/modules/common/ports/input-port';

// Digimon-specific input port that extends base functionality
// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface DigimonInputPort extends InputPort {
  // Digimon-specific input methods can be added here if needed
}
